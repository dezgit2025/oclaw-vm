# 2026-02-16

- Food tracker defaults/notes:
  - User default coffee shots are **regular** (not decaf) unless explicitly stated.

- Created docs + Gmail drafts:
  - Wrote `/home/desazure/.openclaw/workspace/cbot-skill-building.md` (how des decides when to stop clarifying questions; includes recommended skill subfolder architecture) and created Gmail draft to `desi4k@gmail.com` with it attached. Draft: `r-6959500542092062847`.
  - Wrote `/home/desazure/.openclaw/workspace/cbot-tooling-how-i-choose.md` (how des decides which tool/workflow to use; LinkedIn scraping example) and created Gmail draft to `desi4k@gmail.com` with it attached. Draft: `r2529187782747873941`.

- Drive OAuth issue:
  - Upload to OpenClawShared failed with `invalid_grant: Token has been expired or revoked` for `~/.config/openclaw-gdrive/token-openclawshared.json` (needs reauth via loopback + SSH tunnel).

- Built new skill:
  - Initialized + implemented new skill `youtube-shorts` at `/home/desazure/.openclaw/workspace/skills/youtube-shorts/`.
  - Provides `scripts/download_short.sh` wrapper calling `scripts/download_short.py` to download YouTube Shorts to MP4 via `yt-dlp`, with options:
    - `--upload-drive`
    - Gmail drafts: `--gmail-draft` (defaults to `desi4k@gmail.com`) or `--gmail-draft-to <email>`
  - Output dir default: `/home/desazure/.openclaw/workspace/youtube/shorts/out`.
  - Validation script `quick_validate.py` failed due to missing PyYAML (`ModuleNotFoundError: yaml`).

- Preference:
  - Gmail draft default recipient (for Shorts skill): **desi4k@gmail.com**.
