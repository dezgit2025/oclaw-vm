# 2026-02-15

## Google OAuth reauth (consumer Gmail) — what worked
### Key outcome
- Successful Gmail OAuth on headless Azure VM by using **loopback redirect + SSH tunnel** (NOT manual code paste).
- Token created:
  - `~/.config/openclaw-gmail/token-assistantdesi_gmail_com.json`
  - Confirmed `refresh_token` present; scope `gmail.compose`.

### Why the earlier attempt failed
- The “paste the code” / OOB-style flow hit Google error:
  - `Access blocked: Authorization Error`
  - `Missing required parameter: redirect_uri`
- Conclusion: manual code-paste is unreliable/deprecated for consumer Gmail OAuth.

### Exact working steps
1) On laptop, start tunnel:
   - `ssh -N -L 18793:127.0.0.1:18793 <vm>`
2) On VM, run Gmail auth (loopback):
   - `/home/desazure/.openclaw/workspace/.venv-gmail/bin/python /home/desazure/.openclaw/workspace/skills/gmail-drafts/scripts/auth.py --account assistantdesi@gmail.com --port 18793`
3) Open the printed URL in laptop browser, approve. Redirect to `http://127.0.0.1:18793/` completes via tunnel.

### Tooling added
- Added ops helper folder:
  - `/home/desazure/.openclaw/workspace/ops/google-auth/`
    - `audit_google_oauth.py` (offline audit of creds/tokens)
    - `google_reauth.sh` (runs reauth flows consistently)
    - `README.md`
- Updated auth scripts to support headless console flags:
  - Drive/Docs/Sheets/Calendar auth scripts now support `--no-local-server` and `--print-url-only`.
- Updated Gmail auth script:
  - Added `--port` and switched default to loopback server (tunnel-friendly).

## Gmail drafts account preference
- User wants Gmail draft workflow to use `assistantdesi@gmail.com` (not `desi4k@gmail.com`).
- Updated `skills/gmail-drafts` scripts to default tokens per account:
  - Auth writes to `~/.config/openclaw-gmail/token-<account>.json` by default.
  - Draft creation uses `token-<account>.json` by default, with fallback to legacy `token.json`.

## Gmail draft created (verification)
- Draft created using `assistantdesi@gmail.com` token:
  - To: `desi4k@gmail.com`
  - CC: `oadejumobi@gmail.com`
  - Subject: `OpenClaw: manual Copilot (Entra SSO) auth steps`
  - Attachment: `/home/desazure/.openclaw/workspace/copilot-entra-auth-manual.md`
  - DraftId: `r-8852087945494304308`

## Google reauth "one-command" workflow (laptop-assisted)
- User chose Option C style flow: laptop script prints SSH tunnel command, waits for user to confirm (Y), then SSHes into VM to start the loopback OAuth server + reauth.
- Implemented guided laptop helper (copied to laptop by user):
  - VM path: `/home/desazure/.openclaw/workspace/ops/google-auth/laptop_google_reauth.sh`
  - Behavior: prints `ssh -N -L 18793:127.0.0.1:18793 <vm-ssh>`; prompts for `Y`; then runs VM-side `google_reauth.sh`.
- User tested it and confirmed it refreshed tokens for Google/Gmail functions.

## Instagram public download → Drive skill
- Created skill: `instagram-public-to-drive`
  - Path: `/home/desazure/.openclaw/workspace/skills/instagram-public-to-drive/`
  - Script: `scripts/ig_public_to_drive.sh`
  - Function: public IG URL → `yt-dlp` download to `/home/desazure/.openclaw/workspace/instagram/out/` → upload to OpenClawShared Drive.
- Test success on URL `https://www.instagram.com/reel/DUuJv3GEbCL/...`:
  - Uploaded Drive file id: `1U2Ret-uRD261tTtbpgRzkzaqQKSyRGTJ`

## Skills catalog update
- Regenerated `/home/desazure/.openclaw/workspace/skills-catalog.md` (now 64 skills)
- Uploaded timestamped copy:
  - `/home/desazure/.openclaw/workspace/skills-catalog-20260215T071647Z.md`
  - Drive id: `1VBkffmToIf7qhPquqtAGqRjPIOqatC6l`

## Instagram downloads retention
- Added weekly cleanup to keep only last 30 downloads in `/home/desazure/.openclaw/workspace/instagram/out/`:
  - Script: `/home/desazure/.openclaw/workspace/ops/instagram/cleanup_instagram_out.py`
  - Runner: `/home/desazure/.openclaw/workspace/ops/instagram/run_cleanup_instagram_out.sh`
- Scheduled cron: Mondays 5:00 PM America/New_York:
  - `0 17 * * 1 /home/desazure/.openclaw/workspace/ops/instagram/run_cleanup_instagram_out.sh`

## Macro tracker skill request
- User wants a skill to log meals from photo + short description and track daily totals in a Google Sheet (OpenClawShared):
  - calories; protein/carbs/fat; net carbs + total carbs; fiber/sugar/sodium; alcohol; caffeine
  - Prefer exact macros for commonly eaten foods (esp. Starbucks) via a food library; allow manual exact entry overrides.
- Created/updated skill scaffold at `/home/desazure/.openclaw/workspace/skills/macro-tracker/` with:
  - `scripts/food_library.json` (exact items library)
  - `scripts/add_food.py` (upsert exact items)
  - `scripts/log_meal.py` (log meal + totals; optional Sheets append if `nutrition/sheet_id.txt` is set)
