
## Workspace housekeeping and entropy prevention
- Created missing `config/` and `docs/` folders at workspace root.
- Moved loose operational script `log_model_routing.sh` into `ops/scripts/`.
- Moved many configuration, markdown, and miscellaneous docs from workspace root into `docs/` folder for clean org.
- No stray files found outside the main folders (
  skills/, ops/, docs/, memory/, config/
).
- All file movements were logged in `ops/scripts/file_moves.log`.
- These moves keep the workspace aligned with our canonical folder structure (`GLOBAL_FOLDER_STRUCTURE.md`).

[Logged on 2026-02-20]
