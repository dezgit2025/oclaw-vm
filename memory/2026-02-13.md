# 2026-02-13

## Durable notes (OpenClaw ops)
- Set up **gateway watchdog** (no LLM):
  - Scripts:
    - `/home/desazure/.openclaw/workspace/manage-oclaw/gateway_watchdog.sh`
    - `/home/desazure/.openclaw/workspace/manage-oclaw/run_gateway_watchdog.sh`
  - Cron: `*/5 * * * * /home/desazure/.openclaw/workspace/manage-oclaw/run_gateway_watchdog.sh`
  - Behavior: checks `systemctl --user is-active openclaw-gateway.service` + TCP open to `127.0.0.1:18789`; after 2 consecutive fails restarts gateway; 30-min restart rate limit.
  - Logs: `~/.openclaw/logs/gateway-watchdog/YYYY-MM-DD.log`; auto-delete logs older than 3 days.

- Installed **mcporter** MCP CLI on VM:
  - `npm i -g mcporter` → version `0.7.3`.

- Created MCP config directory + config (Option C):
  - `~/.openclaw/mcp/mcporter.json`
  - Added servers:
    - `mslearn` → `https://learn.microsoft.com/api/mcp`
    - `context7` → `https://mcp.context7.com/mcp`

- Added MCP wrapper script for convenience:
  - `~/.openclaw/mcp/mcp.sh` (executes `mcporter --config ~/.openclaw/mcp/mcporter.json ...`).

- Verified Context7 MCP works without API key for basic public-doc queries (within free-tier limits).

## Docs saved
- Wrote gateway explainer doc and uploaded to OpenClawShared:
  - Local: `/home/desazure/.openclaw/workspace/what-oclaw-gateway-does.md`
  - Drive id: `1Fuh_b9xB4RKsOwbmtt-CDHTJYjs1dzSc`
