# OpenClaw upgrades (TODO)

- [ ] Add a **research mode** (no `/research` flag): route “research” requests through a pipeline that uses **Brave `web_search`/`web_fetch`** for retrieval and **Kimi K2.5 (Azure AI Foundry via managed identity)** for synthesis.
- [ ] Confirm whether Brave Search API **Pro** exposes any “forums/discussions” specific feature/parameter beyond higher quota/rate limits (requires checking the dashboard plan details).
